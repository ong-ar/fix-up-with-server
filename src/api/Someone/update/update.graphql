type Mutation {
  SomeOneUpdate(
    id: String!
    access: accessType!
    gender: genderType!
    age: Int!
    height: Int!
    company: companyType!
    etc: String!
  ): Boolean!

  SomeOneUpdateStatus(id: String!, status: statusType!): Boolean!
}
