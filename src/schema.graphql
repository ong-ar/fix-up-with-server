type User {
  id: String!
  email: String!
}

type Query {
  myProfile: User!
}

type Mutation {
  signIn(email: String!, password: String!): String!
  signUp(id: String!, password: String!): Boolean!
}
